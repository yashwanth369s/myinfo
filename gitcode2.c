//This code is to print a string without using a semicolon
#include<stdio.h>
void main(void)
{
/*here printf returns the no of characters in the input
  then excutes printf then empty block without using semicolon
  */ 
    if(printf("product based MNC"))
    {}  

    if(printf("service based MNC"))
    {}
}